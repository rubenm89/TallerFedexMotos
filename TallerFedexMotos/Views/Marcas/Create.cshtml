@model TallerFedexMotos.Models.Marca

@{
    ViewData["Title"] = "Create";
}

<h1>Crear</h1>

<h4>Marca</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="nombreMarca" class="control-label"></label>
                <input asp-for="nombreMarca" class="form-control" />
                <span asp-validation-for="nombreMarca" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Nuevo" class="btn btn-danger" />
            </div>
        </form>
    </div>
</div>

<!--<form asp-action="Importar" enctype="multipart/form-data">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="form-group">
        <label class="control-label">Seleccione un archivo para importar marcas</label>
        <input--> @*asp-for="marca"*@ <!--type="file" id="import" />
    </div>
    <div class="form-group">
        <input type="submit" value="Importar" class="btn btn-primary" />
    </div>
</form>-->
<div class="row">
    <div class="col-md-4">
        Importar datos desde archivo CSV:
        <form asp-action="Importar" enctype="multipart/form-data">
            <div class="form-group">
                <input name="archImportar" id="archImportar" type="file" />
            </div>
            <div class="form-group">
                <input type="submit" value="Importar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Atras</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
